<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.experiment.studentManagement.mapper.UserMapper">

    <insert id="insert" parameterType="com.experiment.studentManagement.model.User" useGeneratedKeys="true" keyProperty="userId">
        insert into user_account (username, password, user_type, role_id, student_id, real_name, created_time, updated_time)
        values (#{username}, #{password}, #{userType}, #{roleId}, #{studentId}, #{realName}, now(), now())
    </insert>

    <delete id="deleteByStudentId" parameterType="int">
        delete from user_account where student_id = #{studentId}
    </delete>

    <update id="updateById" parameterType="com.experiment.studentManagement.model.User">
        update user_account
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            updated_time = now()
        </set>
        where user_id = #{userId}
    </update>

</mapper>